package day07;

public class StringMethod {
    public static void main(String[] args) {
      /*
      there is 2 code error in java
      Compile time error is mistake when writing code
      Run time error exists after the executing code
      ***charAt(i), concat, contains, endWith method,
       equalIgnoreCase, equals, indexOf, indexOf(x, i),
       isEmpty, lastIndexOf, length(), replace(), replaceAll(),
       starsWith, substring, trim(),
       *regular expression(regex) belirli turdeki karakterlerimizi secebilmemize
       olanak verir. \\d == tum rakamlar
                      \\D== tum rakam dışı
                      \\w== tum A, b, 0-9, _
                      \\W== yukarıdakilerin dışındaki
                      \\s== space
                      \\S==space dısındakiler
       String methods
       */
    }
}
